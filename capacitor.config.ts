import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'telephonekhone_ssr',
  webDir: 'www',
  bundledWebRuntime: false
};

export default config;
